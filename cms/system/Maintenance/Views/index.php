<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h3 mb-1">Обслуживание CMS</h1><div class="text-muted">Каждый модуль обслуживает только принадлежащие ему данные.</div></div><form method="post" action="<?= site_url('admin/system/cleanup') ?>"><?= csrf_field() ?><button class="btn btn-primary" onclick="return confirm('Запустить все операции обслуживания?')">Запустить обслуживание</button></form></div>
<?php if (session()->getFlashdata('success')): ?><div class="alert alert-success"><?= esc(session()->getFlashdata('success')) ?></div><?php endif; ?>
<div class="card mb-3"><div class="card-header">Зарегистрированные providers</div><div class="list-group list-group-flush"><?php foreach ($providers as $provider): ?><div class="list-group-item"><strong><?= esc($provider->label()) ?></strong><br><code><?= esc($provider->key()) ?></code></div><?php endforeach; ?></div></div>
<?php $result = session()->getFlashdata('maintenance_result'); if (is_array($result)): ?><div class="card"><div class="card-header">Результат последнего запуска</div><div class="card-body"><pre class="mb-0"><?= esc(json_encode($result, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES)) ?></pre></div></div><?php endif; ?>
